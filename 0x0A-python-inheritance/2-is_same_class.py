#!/usr/bin/python3
"""return True if  object is exactly  instance of  specified class
"""


def is_same_class(obj, a_class):
    """return True if object is exact instance"""
    return True if type(obj) == a_class else False
